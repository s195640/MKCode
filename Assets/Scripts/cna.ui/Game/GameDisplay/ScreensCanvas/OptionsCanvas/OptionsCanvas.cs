using UnityEngine;

namespace cna.ui {
    public class OptionsCanvas : MonoBehaviour {

        //private int seed = 0;
        //private int currentButtonIndex = 0;

        //[SerializeField] private ScoreBoardPanel ScoreBoardPanel;

        //[SerializeField] private CNA_Button[] optionButtons;
        //[SerializeField] private GameObject[] panels;

        //public void SetupUI() {
        //    currentButtonIndex = 0;
        //    optionButtons[currentButtonIndex].Active = false;
        //    panels[currentButtonIndex].SetActive(true);
        //    for (int i = 1; i < optionButtons.Length; i++) {
        //        optionButtons[i].Active = true;
        //        panels[i].SetActive(false);
        //    }
        //    ScoreBoardPanel.SetupUI();
        //    seed = D.GLD.Seed;
        //}

        //public void UpdateUI() {
        //    if (seed != D.GLD.Seed) {
        //        SetupUI();
        //    }
        //    ScoreBoardPanel.UpdateUI();
        //}

        //public void OnClick_Button(int index) {
        //    optionButtons[currentButtonIndex].Active = true;
        //    panels[currentButtonIndex].SetActive(false);
        //    currentButtonIndex = index;
        //    optionButtons[currentButtonIndex].Active = false;
        //    panels[currentButtonIndex].SetActive(true);
        //}
    }
}
